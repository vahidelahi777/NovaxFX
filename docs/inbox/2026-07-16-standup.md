---
title: Standup 2026-07-16
tags: [novaxfx/inbox, standup]
---

# NovaxFX — Daily Standup · 2026-07-16

_PM/architect run. Repo connected. Reconciled task board / admin-plan / capability-map against the actual working tree, not just `git log`._

## Shipped since yesterday
Two commits landed in the last 26h, plus a large **uncommitted** pile on branch `h1-admin-skeleton`:

- **B1 per-user signal fan-out** committed (`43ba01b`): `bot/fanout.py` (`select_recipients` + `format_signal_message`), `bot/delivery.py` (async tier-delayed `deliver`). B2/B3 satisfied by these.
- **DZ2 Postgres user registry + product-bot wiring** committed (`380c1d9`); DZ1/DZ3 deploy fixes (`22ce185`).
- **B5 (wire fan-out into daemon) — code-complete but UNCOMMITTED:** `bot/dispatch.py` (`dispatch_signal`, with an H5 `killswitch` seam) + `tests/test_dispatch.py`; `scripts/prod_daemon_xauusd.py:521` now calls `dispatch_signal(sig, state.repo, _send, primary_session)`.
- **H1 admin skeleton — code-complete but UNCOMMITTED:** `src/novax/admin/` (`app.py` create_app, `auth.py` argon2 + itsdangerous sessions + CSRF + RateLimiter, `services.py` `health_snapshot`, `views/` base/login/dashboard/health) + `scripts/run_admin.py` + `tests/test_admin_{app,auth,services}.py`.
- New docs (untracked): advisory board (CEO/CTO/CPO), `system-capability-map.md`, `admin-panel-plan.md`, `00-NovaxFX-Home.md`, `continuous-agent-team.md`.

## Board status / corrections
- **B5 is done in code but board still shows `🔴 next`.** The daemon already imports and calls `dispatch_signal`. Verify green, commit, then flip B5 → 🟢.
- **H1 is done in code but board/home show "in progress."** Auth, health page, CSRF, rate-limit, base layout all present. Verify green, commit, then flip H1 → 🟢.
- **One-task-per-PR is violated:** B5 (bot fan-out wiring) and H1 (admin panel) and untracked docs are all sitting together in one dirty working tree on `h1-admin-skeleton`. These are two different tasks and must be split.
- A3, B1/B2/B3, DZ1/DZ2/DZ3, A5 correctly marked done. DZ4 (branch protection) still not enabled; DZ5 (aggregator profile) done.

## Recommended next task + why
**H6 — admin audit log** (admin-plan sequences it "audit early: H1 → H6 → H2 → H3 → H5 → H4"). It's additive, pure/wiring-split, and must exist *before* any mutating admin action (H2 set-tier/ban, H5 kill switch) so every operator action is recorded from day one. It builds directly on the just-finished H1 stack. **Precondition:** land B5 and H1 as two separate green PRs first (see blockers).

## Paste-ready prompt

```
You are working in the NovaxFX repo (research-grade FX platform, Python 3.12+).
Implement task H6 of Epic H (admin panel): an append-only admin AUDIT LOG that
records every mutating admin action, plus a view page. Additive changes only.

Project rules (enforce all):
- `from __future__ import annotations`, explicit `__all__`, full type hints,
  UTC-aware datetimes (`datetime.now(UTC)`), `StrEnum` for string enums.
- Pure/wiring split, matching the existing admin package:
  `admin/auth.py` and `admin/services.py` are PURE (no FastAPI, no psycopg,
  no network — all I/O injected via callables); only `admin/app.py` imports
  FastAPI. Keep it that way.
- Never log or print secrets or admin passwords.
- Green, zero new warnings: `ruff check src tests` · `ruff format --check src tests`
  · `mypy src/novax/admin` (strict) · `pytest`.
- Unit tests need NO network and NO live DB (inject fakes / in-memory sinks).

What exists today (read before writing):
- `src/novax/admin/app.py` — `create_app(*, password_hash, session_secret,
  check_db=None, cookie_secure=True)`; helpers `_get_identity`, `generate_csrf`/
  `verify_csrf`; routes `/login`, `/logout`, `/` (dashboard), `/health`.
- `src/novax/admin/auth.py` — argon2 password, itsdangerous sessions, CSRF,
  `RateLimiter` (pure).
- `src/novax/admin/services.py` — pure query layer; `health_snapshot(check_db)`
  returns a `TypedDict`. Pattern: inject I/O as callables.
- `src/novax/admin/views/` — `base.html`, `login.html`, `dashboard.html`,
  `health.html` (Jinja2 + HTMX).
- `src/novax/bot/db_postgres.py` — `PostgresUserRepository` + `SCHEMA_SQL`;
  copy its psycopg + `SCHEMA_SQL` idempotent-DDL style for the audit table.

Requirements:
1. New PURE module `src/novax/admin/audit.py`:
   - A frozen `AuditEntry` dataclass: `ts: datetime` (UTC-aware), `actor: str`,
     `action: str` (use a `StrEnum` e.g. `AuditAction`: LOGIN, LOGOUT,
     SET_TIER, BAN_USER, UNBAN_USER, KILL_SWITCH_ON, KILL_SWITCH_OFF,
     BROADCAST), `target: str | None`, `before: str | None`, `after: str | None`.
   - An `AuditSink` Protocol: `append(entry: AuditEntry) -> None` and
     `recent(limit: int = 100) -> list[AuditEntry]`.
   - An `InMemoryAuditSink` implementing it (for tests + dev).
   - A pure helper `record(sink, *, actor, action, target=None, before=None,
     after=None, now=...) -> AuditEntry` that builds the entry (UTC now
     injectable) and appends it. No FastAPI, no psycopg imports here.
2. Postgres persistence in a SEPARATE wiring module
   `src/novax/admin/audit_postgres.py` (only place that imports psycopg here):
   `PostgresAuditSink` implementing `AuditSink`, plus idempotent `SCHEMA_SQL`
   (`admin_audit_log` table: id, ts timestamptz, actor, action, target,
   before, after) mirroring `db_postgres.py` conventions.
3. Wire into `admin/app.py`:
   - `create_app` accepts an injected `audit: AuditSink | None = None`
     (default `InMemoryAuditSink()`).
   - Record LOGIN (success only) and LOGOUT via the audit sink.
   - Add a protected `GET /audit` route (redirect to `/login` if no identity)
     rendering a new `views/audit.html` (extends `base.html`) listing
     `audit.recent()` newest-first with a CSRF-safe layout; add an "Audit"
     link to `base.html` nav.
4. Tests `tests/test_admin_audit.py` (no network, no DB): `record()` builds a
   correct UTC entry and appends; `InMemoryAuditSink.recent(limit)` returns
   newest-first and respects the limit; `AuditAction` is a `StrEnum`; assert
   `audit.py` imports without FastAPI/psycopg installed (pure module). Add a
   FastAPI `TestClient` smoke test: login → `/audit` renders 200 and shows the
   LOGIN entry; unauthenticated `/audit` redirects to `/login`.

Before finishing, run and report:
`ruff check src/novax/admin tests` · `ruff format --check src/novax/admin tests`
· `mypy src/novax/admin` · `pytest tests/test_admin_app.py tests/test_admin_auth.py tests/test_admin_services.py tests/test_admin_audit.py`.
All must be green. Then summarize the diff and the new routes/tables added.
```

## Risks / blockers
- **#1 — Uncommitted, mixed working tree.** B5 (bot fan-out) + H1 (admin) + new docs are all uncommitted on `h1-admin-skeleton`, and none is verified green in CI. Split into two clean PRs — **B5 first** (it's earlier in the Phase-0 order and revenue-critical), then H1 — each with green ruff/mypy(strict)/pytest, before stacking H6. Nothing is "done" until committed and green.
- **Kill-switch dependency (D-001 / K2).** `dispatch.py` already exposes a `killswitch` seam; H5 will wire the real flag. The kill switch MUST ship before any live/auto execution (Epic X) — do not sequence broker work ahead of it.
- **Ops/compliance still open:** DZ4 branch protection not enabled on `main`; G1 (disclaimer on every surface) still 🔴; legal review required before charging money or enabling live execution.
